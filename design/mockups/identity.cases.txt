identity.full-en — the five-second screen in the default language: wordmark with the hearth-O, identity line, the street address, an hours line whose each half owns its own days and is true any night without JavaScript, the Google rating with the review count that proves it, and the booking button all readable in the first view; the hero arch holds the authored hearth drawing, so the first screen carries no season — the one borrowed December photo sits below the fold under a caption that names its month and everything in its frame; no horizontal scroll at any width from 320 to 375px, and the page requests no alphabet its own copy does not read.
identity.full-ka — Georgian throughout: no capitals exist, words run long; labels must rank without uppercase, titles at the ka size (22px; 19px under 360px) must not break words, the Latin brand sits beside Georgian text without clashing, the button above the fold sits at the same height as on the other two pages at 375 and 360px (at 320px the address wraps to two lines and the button sits 23.3px lower — disclosed, the same on ru), and no line opens with an em dash down to 320px.
identity.full-ru — Russian at its widest: the CTA label, the hours line and the longest menu lines (“Драники с беконом и сметаной”) still fit one 375px column, and the tagline holds its four lines with a full 20px of shrink slack.
identity.longest-georgian — constructed, not sampled: a 48-glyph dish name of the widest Georgian letters (ჭღწშძთ), a single unbroken 20-glyph word as a section title and a group header, a tagline of three 20-glyph words; nothing overflows and the 20-glyph title never breaks mid-word at any width from 320 to 375px, and every price stays attached to its leader.
identity.desktop — the same document as identity.full-en, measured at 1280px: one reading column (max 680px), larger display lockup, two-up photo grid; no second layout idea appears, and no second file exists to drift.
identity.menu-awkward — the data as the venue actually wrote it, on the English page: a Russian-only hookah at 190 ₾, Cappuccino with stray quotes, the mashed segment Holodnikცივი, a 49-character wine line (with the venue’s own Cyrillic “е” in “Tеliani”) that must wrap with its leader intact, prices from 5 to 190 aligning in one column; the foreign-script lines take the system stack so the page still downloads only Latin.
identity.photo-denied — every borrowed photo withdrawn (the owner may refuse them): the hero arch holds the authored drawing of the hearth — a mortared brick ring with depth behind it and a sketched flame, its own composition, not the logo rescaled — flanked by the shelf and book stacks; every atmosphere drawing contains what its caption names (the game boxes, the croutons and the typewriter included); the borrowed-photos footer line is gone; and the first screen still reads dark, warm and book-lined.
