# Where this skill comes from

Vendored from
[nextlevelbuilder/ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)
at commit `bc826e2267a36d98a2dcf5231e16c30ff546770f` (MIT licence, upstream
holds the text). Taken: `SKILL.md`, `data/`, `references/`, and the top-level
`scripts/*.py`. Left behind: `scripts/tests/` with its fixtures — they test
the skill itself, and this repository is a consumer; re-fetch the commit above
to run them.

Local changes to the vendored files are limited to the **"Local note"**
section appended to `SKILL.md`; everything else is upstream verbatim. To
update: fetch a newer commit, re-copy the same set, re-append the note, and
update the commit hash here.

This machine has no Python, which the upstream search tool requires —
[TECH-DEBT.md](../../../TECH-DEBT.md) holds that entry and its trigger. Until
then the note in `SKILL.md` routes reads straight at `data/*.csv`.
